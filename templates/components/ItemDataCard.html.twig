<div{{ attributes }}>
 
    <article class="bg-white border-l-4 border-gray-700 rounded-2xl shadow p-4 flex justify-between items-center">
        <div>
            <p class="text-sm text-gray-500">{{ title}}</p>
            <h2 class="text-3xl font-bold">{{ value}}</h2>
        </div>
        <div class="{{ colorIcon }} rounded-xl p-1 ">
            <img src="{{ iconPath }}" alt="" class="w-12 h-12">
        </div>
    </article>
</div>
